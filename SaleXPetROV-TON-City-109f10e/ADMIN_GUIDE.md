# TON City Builder - Инструкции администратора

## 📋 Содержание
1. [Вход в админ-панель](#вход-в-админ-панель)
2. [Настройка TON кошелька](#настройка-ton-кошелька)
3. [Управление пополнениями](#управление-пополнениями)
4. [Деплой проекта](#деплой-проекта)
5. [Смарт-контракт TON](#смарт-контракт-ton)

---

## 🔐 Вход в админ-панель

### Шаг 1: Создание админа

**Первый пользователь становится админом автоматически.**

Если нужно назначить другого админа:

```bash
# Подключитесь к MongoDB
mongo

# Используйте базу данных
use test_database

# Назначьте пользователя админом
db.users.updateOne(
  { wallet_address: "EQxxxYOUR_WALLET_ADDRESSxxx" },
  { $set: { is_admin: true } }
)
```

### Шаг 2: Вход

1. Откройте приложение: `http://localhost:3000` (или ваш домен)
2. Нажмите "Connect Wallet"
3. Подключите TON кошелек через TON Connect
4. После подключения перейдите: `http://localhost:3000/admin`

**Важно:** Только пользователи с `is_admin: true` могут видеть админ-панель.

---

## 💰 Настройка TON кошелька

### Вкладка "TON Кошелек" в админке

#### 1. Выбор сети

**Testnet (для тестирования):**
- Используйте для разработки и тестирования
- Тестовые TON не имеют ценности
- Получить тестовые TON: https://t.me/testgiver_ton_bot

**Mainnet (реальные деньги):**
- Используется для продакшна
- Реальные TON с реальной стоимостью
- ⚠️ **ВНИМАНИЕ:** Будьте осторожны!

#### 2. Адрес для получения платежей

Это ваш основной кошелек, куда будут приходить все платежи от игроков.

**Как получить адрес:**

1. Откройте TON кошелек (Tonkeeper, TON Space, etc.)
2. Скопируйте ваш адрес получения
3. Вставьте в поле "Адрес для получения платежей"
4. Нажмите "Сохранить настройки"

**Формат адреса:**
```
EQDtFpEwcFAEcRe5mLVh2N6C0x-_hJEM7W61_JLnSF74p4q2
```

#### 3. Как это работает

```
Игрок → Отправляет TON → Ваш кошелек
                ↓
        Backend мониторит
                ↓
    Автоматически зачисляет
    на внутренний баланс игрока
```

**Преимущества:**
- ✅ Реальное пополнение TON
- ✅ Внутренние транзакции без комиссий
- ✅ Быстрая торговля между игроками
- ✅ Вы контролируете основной кошелек

---

## 📊 Управление пополнениями

### Автоматическое зачисление

Система автоматически:
1. Мониторит входящие транзакции на ваш адрес
2. Находит пользователя по адресу отправителя
3. Зачисляет TON на внутренний баланс
4. Записывает транзакцию в историю

### Ручное зачисление

Если транзакция не зачислилась автоматически:

**В админке:**
1. Перейдите в "TON Кошелек"
2. Найдите транзакцию в "История пополнений"
3. Если статус "Ожидание" - можно зачислить вручную

**Через API:**
```bash
curl -X POST http://localhost:8001/api/admin/deposits/{tx_hash}/credit \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{
    "wallet_address": "EQxxx...",
    "amount_ton": 10.5
  }'
```

### Просмотр статистики

В разделе "История пополнений" вы увидите:
- Все транзакции пополнения
- Адреса отправителей
- Суммы в TON
- Статусы (Завершено/Ожидание)
- Даты и время

---

## 🚀 Деплой проекта

### Вариант 1: Emergent Platform (Рекомендуется)

**Текущая платформа уже предоставляет:**
- ✅ Хостинг frontend и backend
- ✅ MongoDB база данных
- ✅ Автоматические обновления
- ✅ SSL сертификаты

**Для деплоя:**
1. Используйте функцию "Deploy" в Emergent
2. Платформа автоматически соберет и задеплоит проект

### Вариант 2: VPS/Dedicated Server

#### Требования:
- Ubuntu 20.04+ или аналог
- Node.js 18+
- Python 3.9+
- MongoDB 5.0+
- Nginx (для reverse proxy)
- 2GB RAM минимум
- SSL сертификат

#### Установка:

```bash
# 1. Клонировать проект
git clone <your-repo-url>
cd ton-city-builder

# 2. Установить зависимости Backend
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 3. Настроить .env
cp .env.example .env
nano .env
# Установите MONGO_URL и другие переменные

# 4. Установить зависимости Frontend
cd ../frontend
yarn install

# 5. Собрать Frontend
yarn build

# 6. Настроить Nginx
sudo nano /etc/nginx/sites-available/ton-city-builder
```

**Nginx конфигурация:**
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    # Frontend
    location / {
        root /path/to/frontend/build;
        try_files $uri /index.html;
    }
    
    # Backend API
    location /api {
        proxy_pass http://localhost:8001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
    
    # WebSocket
    location /ws {
        proxy_pass http://localhost:8001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
    }
}
```

```bash
# 7. Включить сайт
sudo ln -s /etc/nginx/sites-available/ton-city-builder /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# 8. Настроить systemd для Backend
sudo nano /etc/systemd/system/ton-city-backend.service
```

**Systemd service:**
```ini
[Unit]
Description=TON City Builder Backend
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/backend
Environment="PATH=/path/to/backend/venv/bin"
ExecStart=/path/to/backend/venv/bin/uvicorn server:app --host 0.0.0.0 --port 8001
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# 9. Запустить сервис
sudo systemctl daemon-reload
sudo systemctl enable ton-city-backend
sudo systemctl start ton-city-backend

# 10. Настроить SSL с Let's Encrypt
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d yourdomain.com
```

### Вариант 3: Vercel (Frontend) + Railway/Render (Backend)

#### Frontend на Vercel:

1. Подключите GitHub репозиторий
2. Настройте Build:
   - Build Command: `cd frontend && yarn build`
   - Output Directory: `frontend/build`
3. Environment Variables:
   - `REACT_APP_BACKEND_URL=https://your-backend.railway.app`

#### Backend на Railway:

1. Создайте новый проект
2. Подключите репозиторий
3. Добавьте MongoDB (или используйте MongoDB Atlas)
4. Environment Variables:
   - `MONGO_URL=mongodb://...`
   - `DB_NAME=ton_city_builder`
   - `CORS_ORIGINS=https://your-frontend.vercel.app`

---

## 🔗 Смарт-контракт TON

### Файл контракта

Смарт-контракт находится в `/app/contracts/payment_receiver.fc`

### Функции контракта

**Основные возможности:**
- Принимает входящие TON платежи
- Читает комментарий/memo из транзакции
- Хранит баланс
- Безопасно обрабатывает bounced сообщения

### Компиляция контракта

**Требования:**
- TON development tools
- func compiler
- fift

```bash
# Установка TON tools
brew tap ton-blockchain/ton
brew install ton

# Компиляция контракта
func -o payment_receiver.fif -SPA contracts/payment_receiver.fc
fift -s payment_receiver.fif
```

### Деплой контракта

**Через TON Wallet:**

1. Скомпилируйте контракт
2. Получите `.boc` файл
3. Задеплойте через Tonkeeper или TON Space
4. Получите адрес контракта

**Через API:**

```javascript
import { TonClient, WalletContractV4, internal } from "ton";
import { mnemonicToPrivateKey } from "ton-crypto";

// Подключение к сети
const client = new TonClient({
    endpoint: 'https://toncenter.com/api/v2/jsonRPC',
});

// Деплой контракта
async function deployContract() {
    const mnemonic = "your 24 words...";
    const key = await mnemonicToPrivateKey(mnemonic.split(" "));
    
    const wallet = WalletContractV4.create({
        publicKey: key.publicKey,
        workchain: 0
    });
    
    // ... деплой логика
}
```

### Использование в админке

После деплоя контракта:
1. Скопируйте адрес контракта
2. Вставьте в админку как "Адрес для получения платежей"
3. Система начнет мониторить этот адрес

---

## ❓ FAQ

### Как переключиться с testnet на mainnet?

1. Зайдите в админку
2. Вкладка "TON Кошелек"
3. Выберите "Mainnet" в dropdown
4. Введите mainnet адрес кошелька
5. Сохраните

### Как получить тестовые TON?

Telegram бот: https://t.me/testgiver_ton_bot

Команда: `/give <your_testnet_address>`

### Как проверить что мониторинг работает?

1. Отправьте тестовый TON на настроенный адрес
2. Подождите 30-60 секунд
3. Проверьте "История пополнений" в админке
4. Транзакция должна появиться автоматически

### Что делать если пополнение не зачислилось?

1. Проверьте логи backend: `/var/log/supervisor/backend.err.log`
2. Убедитесь что адрес настроен правильно
3. Проверьте что пользователь зарегистрирован в игре
4. При необходимости зачислите вручную через админку

### Как вывести средства с game кошелька?

Система уже имеет функцию withdrawals:
1. Игрок запрашивает вывод
2. Админ видит запрос в "Pending Withdrawals"
3. Админ одобряет
4. Отправляете TON с основного кошелька вручную
5. Подтверждаете в системе

---

## 📞 Техническая поддержка

**Логи:**
- Backend: `/var/log/supervisor/backend.err.log`
- Frontend: Browser console (F12)
- MongoDB: `mongod.log`

**Полезные команды:**
```bash
# Перезапуск сервисов
sudo supervisorctl restart backend
sudo supervisorctl restart frontend

# Проверка статуса
sudo supervisorctl status

# Просмотр логов в реальном времени
tail -f /var/log/supervisor/backend.err.log
```

**База данных:**
```javascript
// MongoDB commands
use ton_city_builder

// Посмотреть всех админов
db.users.find({ is_admin: true })

// Посмотреть настройки кошелька
db.game_settings.find({ type: "ton_wallet" })

// Посмотреть пополнения
db.deposits.find().sort({ created_at: -1 }).limit(10)
```

---

## ✅ Чеклист запуска

- [ ] Backend запущен
- [ ] Frontend запущен
- [ ] MongoDB работает
- [ ] Создан первый админ
- [ ] Вход в админку успешен
- [ ] Настроен адрес кошелька
- [ ] Выбрана сеть (testnet/mainnet)
- [ ] Проведена тестовая транзакция
- [ ] Пополнение автоматически зачислилось
- [ ] SSL сертификат установлен (для продакшна)
- [ ] Домен настроен
- [ ] Backup базы данных настроен

---

**Готово!** Ваша игра готова к приему реальных TON платежей! 🚀
