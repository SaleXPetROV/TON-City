# 📁 Структура проекта TON City

## ✅ Правильная структура (текущая)

```
/app/
├── README.md                    # Главное описание проекта
├── TESTING_GUIDE_RU.md         # Руководство по тестированию
├── .gitignore                  # Игнорируемые файлы
├── .git/                       # Git репозиторий
│
├── backend/                    # Backend (FastAPI)
│   ├── server.py              # Главный файл приложения
│   ├── background_tasks.py    # Фоновые задачи (cron)
│   ├── payment_monitor.py     # Мониторинг платежей
│   ├── ton_integration.py     # Интеграция с TON
│   ├── requirements.txt       # Python зависимости
│   ├── .env                   # Переменные окружения
│   └── venv/                  # Virtual environment
│
├── frontend/                   # Frontend (React)
│   ├── public/                # Статические файлы
│   │   ├── index.html
│   │   └── tonconnect-manifest.json
│   ├── src/                   # Исходный код
│   │   ├── App.js            # Главный компонент
│   │   ├── App.css
│   │   ├── pages/            # Страницы
│   │   ├── components/       # Компоненты
│   │   ├── lib/             # Утилиты
│   │   └── translations/     # Переводы
│   ├── package.json          # Node зависимости
│   ├── yarn.lock            # Lock файл Yarn
│   ├── tailwind.config.js   # Tailwind настройки
│   └── .env                 # Переменные окружения
│
└── tests/                     # Тесты
    └── __init__.py
```

## ❌ Неправильная структура (было)

```
/app/
├── backend/
├── frontend/
└── SaleXPetROV-TON-City-109f10e/  # ❌ Дублирующая папка
    ├── backend/                    # ❌ Дублирование
    ├── frontend/                   # ❌ Дублирование
    └── ...
```

## 🔧 Что было исправлено

1. ✅ Удалена вложенная папка `SaleXPetROV-TON-City-109f10e`
2. ✅ Удалены архивы `ton-city.tar.gz` и `ton-city.zip`
3. ✅ Удалён лишний `yarn.lock` в корне
4. ✅ Обновлён `README.md` с правильным описанием
5. ✅ Структура теперь чистая и понятная

## 📊 Статистика проекта

**Backend:**
- Python файлы: 4
- Основные модули: server.py, ton_integration.py, payment_monitor.py, background_tasks.py

**Frontend:**
- Страницы: GamePage, TradingPage, DashboardPage, AdminPage, LandingPage
- Компоненты: TutorialModal, BalanceModals, WalletSettings и др.
- Система переводов: 3 языка (EN, RU, ZH)

**База данных:**
- MongoDB коллекции: users, plots, businesses, trade_contracts, transactions

## 🚀 Как работать с проектом

### Локальная разработка

```bash
# 1. Backend
cd /app/backend
source venv/bin/activate  # если нужно
python server.py

# 2. Frontend
cd /app/frontend
yarn start

# 3. Через supervisor (на сервере)
sudo supervisorctl restart all
```

### Git операции

```bash
# Проверить статус
cd /app && git status

# Добавить изменения
git add .

# Коммит
git commit -m "Your message"

# Пуш (авто-деплой на Emergent)
git push origin main
```

### Проверка структуры

```bash
# Список файлов в git
git ls-tree -r --name-only HEAD

# Структура проекта
tree -L 2 -I 'node_modules|__pycache__|.git'

# Размер директорий
du -sh /app/*
```

## 📝 Важные файлы

### Backend
- `server.py` - FastAPI приложение, все эндпоинты
- `.env` - MONGO_URL, DB_NAME, CORS_ORIGINS
- `requirements.txt` - Python зависимости

### Frontend
- `src/App.js` - Главный компонент, роутинг
- `src/pages/GamePage.jsx` - Основная игровая страница
- `.env` - REACT_APP_BACKEND_URL
- `package.json` - Node зависимости

### Документация
- `README.md` - Описание проекта
- `TESTING_GUIDE_RU.md` - Руководство по тестированию

## 🔍 Полезные команды

```bash
# Найти все файлы проекта
find /app -type f -name "*.py" | grep -v venv | grep -v node_modules

# Найти все компоненты React
find /app/frontend/src -name "*.jsx" -o -name "*.js"

# Проверить размер проекта
du -sh /app

# Количество строк кода
find /app -name "*.py" -o -name "*.js" -o -name "*.jsx" | xargs wc -l
```

---

**Проект готов к работе! Структура чистая и организованная. 🎉**
